FROM node:20-slim

WORKDIR /app/bot

COPY bot/package*.json ./
RUN npm ci --omit=dev

COPY bot/ .

EXPOSE 3001
CMD ["sh", "-c", "mkdir -p /app/data/logs && node bot.js 2>&1 | tee -a /app/data/logs/bot.log"]
